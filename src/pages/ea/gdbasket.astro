---
import Layout from '../../layouts/Layout.astro';

// content port จาก intensivetrader.com/src/pages/ea/gdbasket.astro (เดิม)
const MAIN = 'https://intensivetrader.com';
const JOURNAL = 'https://journal.intensivetrader.com/';
const DL_LATEST = `${MAIN}/d/gdbasket`; // ปุ่มหลัก — redirect ชี้ไฟล์รุ่นล่าสุด v3.21 (publish/releases/v3.21/gdBasket_v3.21.ex5)
const DL_V311 = `${MAIN}/d/gdbasket-v311`;
const DL_V310 = `${MAIN}/d/gdbasket-v310`;
const DL_V3092 = `${MAIN}/d/gdbasket-v3092`;
const DL_V202 = `${MAIN}/d/gdbasket-v202`;
const DL_V201 = `${MAIN}/d/gdbasket-v201`;
const DL_V200 = `${MAIN}/d/gdbasket-v200`;
const DL_V137 = `${MAIN}/d/gdbasket-v137`;
const DL_V118 = `${MAIN}/d/gdbasket-v118`;
const XM_AFF = 'https://affs.click/jih0m';
const XM_CODE = 'KWASATI';
const VPS_AFF = 'https://bit.ly/MTCloudVPS';
const COPYTRADE_URL = 'https://social.tp-redirect.com/s/0mYnzepR';

// Myfxbook — ดึงทุกพอร์ต live จาก /api/myfxbook.json ตอน build + fallback ต่อบัญชีกันพัง
// client script ท้ายไฟล์ refresh ค่าสดตอน runtime + clone การ์ดเพิ่มเองเมื่อ API คายพอร์ตใหม่ ไม่ต้องแก้ไฟล์นี้อีก
type MfbCard = {
  id: string; name: string; label: string; url: string | null; newNote: string | null;
  gain: string | null; absGain: string | null; monthly: string | null; drawdown: string | null; balance: string | null;
};
const mfbSlug = (s: string) => (s || 'account').toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/(^-|-$)/g, '');
const mfbFallback: MfbCard[] = [
  { id: '12090186', name: 'gdbasket-xs', label: 'พอร์ต 1', url: 'https://www.myfxbook.com/members/kwasati/gdbasket-xs/12090186', newNote: null, gain: '53.40', absGain: '53.45', monthly: '53.40', drawdown: '18.16', balance: '460.36' },
  { id: '12105756', name: 'gdbasket lx', label: 'พอร์ต 2', url: 'https://www.myfxbook.com/members/kwasati/gdbasket-lx/12105756', newNote: 'พอร์ตใหม่ เริ่ม 2026-07', gain: null, absGain: null, monthly: null, drawdown: null, balance: null },
];
let mfbCards: MfbCard[] = mfbFallback;
try {
  const raw = await fetch('https://www.intensivetrader.com/api/myfxbook.json').then((r) => r.json());
  // API รุ่นเก่าคาย object พอร์ตเดียว (ไม่มี id/name) — แปลงเป็น array โดยถือเป็นพอร์ต 1; รุ่นใหม่คาย array ทุกพอร์ต live
  const accounts = Array.isArray(raw) ? raw : (raw && raw.gain != null ? [{ id: mfbFallback[0].id, name: mfbFallback[0].name, ...raw }] : []);
  if (accounts.length > 0) {
    const f = (n: unknown) => (n == null ? null : Number(n).toFixed(2));
    const toCard = (acc: any, base: MfbCard): MfbCard => ({
      ...base,
      id: String(acc.id),
      name: acc.name || base.name,
      url: `https://www.myfxbook.com/members/kwasati/${mfbSlug(acc.name || base.name)}/${acc.id}`,
      newNote: null,
      gain: f(acc.gain), absGain: f(acc.absGain), monthly: f(acc.monthly), drawdown: f(acc.drawdown), balance: f(acc.balance),
    });
    const used = new Set<string>();
    mfbCards = mfbFallback.map((fb) => {
      const acc = accounts.find((a: any) => String(a.id) === fb.id || (a.name || '').toLowerCase() === fb.name.toLowerCase());
      if (!acc) return fb; // fallback ต่อบัญชี — API ยังไม่คืนพอร์ตนี้
      used.add(String(acc.id));
      return toCard(acc, fb);
    });
    // พอร์ตใหม่ที่ยังไม่รู้จัก — ต่อท้ายเป็นการ์ดเพิ่ม
    for (const acc of accounts) {
      if (!used.has(String(acc.id))) mfbCards.push(toCard(acc, { id: '', name: '', label: `พอร์ต ${mfbCards.length + 1}`, url: null, newNote: null, gain: null, absGain: null, monthly: null, drawdown: null, balance: null }));
    }
  }
} catch (e) { /* ใช้ fallback */ }

const features = [
  { title: 'Adaptive Grid Behavior', desc: 'ตรวจจับว่าราคาอยู่โซนไหน แล้วปรับระยะ Grid และขนาด Lot ให้เหมาะกับสถานการณ์ ตลาดเป็นใจจะเปิดโอกาสให้กำไรโตขึ้น ตลาดไม่เป็นใจจะเปลี่ยนมาเล่นอย่างระมัดระวังมากขึ้น', tag: 'ปรับ Grid + Lot ตามโซนราคา' },
  { title: 'Market Temperature', desc: 'วัดความร้อนของตลาดก่อนเปิดรอบใหม่ ถ้าความผันผวนอันตรายเกินเกณฑ์ ระบบจะงดเข้าเทรดและรอให้ตลาดกลับมาเหมาะสมกว่า', tag: 'กรองอันตรายก่อนเริ่มกอง' },
  { title: 'PPTS', desc: 'เมื่อราคาเดินถูกทาง ระบบจะค่อยๆ เติมไม้เพื่อเพิ่มโอกาสทำกำไร พร้อมไล่ล็อกกำไรรวมของทั้งชุด ให้กำไรวิ่งต่อโดยไม่ปล่อยของที่เก็บมาแล้วไหลกลับมากเกินไป', tag: 'เติมไม้ขาขึ้น + ไล่ล็อกกำไร' },
];

const howSteps = [
  '<strong class="text-gray-light">เริ่มรอบเมื่อสภาพตลาดเหมาะ</strong> ระบบดูอุณหภูมิตลาดก่อน ไม่รีบเปิดกองในจังหวะที่อันตรายเกินเกณฑ์',
  '<strong class="text-gray-light">อ่านโซนราคา</strong> ปรับวิธีวาง Grid และขนาด Lot ตามตำแหน่งราคาและความเหวี่ยงของตลาด',
  '<strong class="text-gray-light">ราคาลง ระบบทยอยซื้อ</strong> วางไม้เพิ่มตามระยะที่คำนวณไว้ เพื่อขยับราคาเฉลี่ยของทั้งกอง',
  '<strong class="text-gray-light">ราคาขึ้น ระบบเติมกำไร</strong> PPTS เติมไม้ตามทาง พร้อมไล่ล็อกกำไรรวมขึ้นไป',
  '<strong class="text-gray-light">ปิดทั้งชุด</strong> เมื่อราคาแตะ Common-TP โบรกจะปิดทุกไม้ในรอบพร้อมกัน',
];

// "กว่าจะมาเป็น VELOCE26" — 5 ขั้น (ย่อ story 5 บท)
const veloceSteps = [
  'ลองมาแล้วแทบทุกทาง — ปรับขนาดไม้ตามความผันผวน / อ่านอารมณ์ตลาดสลับโหมด / กรองก่อนเข้าไม้ / กู้กองตอนราคาไหลแรง — <strong class="text-gray-light">ตัดทิ้งทุกอันที่พิสูจน์แล้วว่าไม่ช่วยจริง</strong>',
  'เหลือ <strong class="text-gray-light">2 บ้าน</strong>ที่พิสูจน์แล้วว่าคุ้ม — เคยลองตัดบ้านล่างทิ้งเพราะกลัวมันทำให้ล้างพอร์ตบ่อย พอลองจริงกำไรหายไปเกินสามในสี่ส่วน จึงรู้ว่าตัดออกไม่ได้',
  'จุดเปลี่ยนสำคัญที่สุด: คุม <strong class="text-gray-light">"เวลาอมกอง"</strong> ไม่ใช่เดาวันร้าย — วันตลาดดิ่งแรงจนไม่เด้งเป็นสิ่งที่คุมไม่ได้ แต่ปิดรอบให้ไวที่สุดคุมได้ ยิ่งมือว่างมาก ยิ่งรอด',
  'เคาะแชมป์บนสนามทดสอบยาว 6 ปี 8 เดือน — สูตรที่ชนะทุกด้านพร้อมกัน กำไรมากกว่าสูตรก่อนหน้า และจำนวนครั้งที่ล้างพอร์ตน้อยกว่าเกือบครึ่ง',
  'ตรึงเป็นค่าตั้งต้น = <strong class="text-gray-light">VELOCE26</strong> — เปิดมาก็เจอค่าที่ผ่านการพิสูจน์มาแล้วทันที ไม่ต้องมานั่งเดาว่าจะตั้งค่ายังไงให้ดี',
];

const releases = [
  { version: 'v3.21', dateLabel: '25 ก.ย. 2026', latest: true, url: DL_LATEST },
  { version: 'v3.11', dateLabel: '10 ก.ย. 2026', latest: false, url: DL_V311 },
  { version: 'v3.10', dateLabel: '22 ส.ค. 2026', latest: false, url: DL_V310 },
  { version: 'v3.092', dateLabel: '19 ส.ค. 2026', latest: false, url: DL_V3092 },
  { version: 'v2.02', dateLabel: '13 ก.ค. 2026', latest: false, url: DL_V202 },
  { version: 'v2.01', dateLabel: '10 ก.ค. 2026', latest: false, url: DL_V201 },
  { version: 'v2.00', dateLabel: '8 ก.ค. 2026', latest: false, url: DL_V200 },
  { version: 'v1.37', dateLabel: '24 มิ.ย. 2026', latest: false, url: DL_V137 },
  { version: 'v1.18', dateLabel: '13 มิ.ย. 2026', latest: false, url: DL_V118 },
];

// file-list rows (github metaphor -> สารบัญ EA)
const fileRows = [
  { icon: 'doc', name: 'ภาพรวม gdBasket', note: 'Adaptive Buy Grid Behavior EA · ทยอยซื้อเป็นชุด ปิดรับกำไรทั้งชุด', meta: 'อ่าน', href: '#readme', color: 'text-gray' },
  { icon: 'gear', name: 'วิธีติดตั้ง', note: 'ลงใน MT5 · ลากใส่กราฟ GOLD M15 · 6 ขั้นจบ', meta: '6 ขั้น', href: '#install', color: 'text-secondary' },
  { icon: 'modes', name: 'เครื่องยนต์ VELOCE26', note: 'ค่าพร้อมใช้ที่ผ่านการทดสอบมาแล้ว · ไม่ต้องเลือกโหมด', meta: 'พร้อมใช้', href: '#modes', color: 'text-primary' },
  { icon: 'star', name: 'จุดเด่น', note: 'Adaptive Grid / Market Temperature / PPTS / Common-TP', meta: 'อ่าน', href: '#features', color: 'text-primary' },
  { icon: 'risk', name: 'ความเสี่ยงที่ต้องรู้', note: 'Buy Grid · Capital · เงินค้ำ · ข้อจำกัดก่อนใช้', meta: 'สำคัญ', href: '#risk', color: 'text-secondary' },
  { icon: 'clock', name: 'Releases', note: 'v3.21 · v3.11 · v3.10 · v3.092 · v2.02 · v2.01 · v2.00 · v1.37 · v1.18', meta: '9 rel', href: '/ea/gdbasket/changelog', color: 'text-gray' },
  { icon: 'calc', name: 'วางแผนเงิน Rolling Capital', note: 'เครื่องคิดเลขถอน/ทบจากผลจริงย้อนหลัง', meta: 'ลองเลย', href: '/ea/gdbasket/rolling-capital', color: 'text-secondary' },
  { icon: 'dl', name: 'gdBasket v3.21', note: 'พร้อมใช้ · โหลดฟรี', meta: 'ดาวน์โหลด', href: DL_LATEST, color: 'text-green', download: true },
  { icon: 'journal', name: 'Trading Journal', note: 'ผลเทรดจริงรายวัน · กำไร/ขาดทุนต่อรอบ', meta: 'ดูประวัติ', href: JOURNAL, color: 'text-primary', accent: true },
];

const installSteps = [
  'ดาวน์โหลดไฟล์ <strong>gdBasket v3.21</strong> จากปุ่มด้านบน',
  'เปิด <strong>MetaTrader 5</strong> → <strong>File → Open Data Folder</strong>',
  'วางไฟล์ในโฟลเดอร์ <code class="install-path">MQL5 → Experts</code>',
  'ปิดเปิด MT5 ใหม่ หรือกด <strong>Refresh</strong> ใน Navigator',
  'ลาก <strong>gdBasket</strong> ใส่กราฟ <strong>GOLD M15</strong> → ติ๊กอนุญาตเทรดอัตโนมัติ → กดปุ่ม <strong>Algo Trading</strong> ให้ขึ้น<strong class="text-green">สีเขียว</strong>',
  'ตั้ง <strong>Capital</strong> = เงินที่ยอมเสียได้ต่อรอบ — Standard ขั้นต่ำ <strong>$1,000</strong> · Micro ขั้นต่ำ <strong>$100</strong> (ต่ำกว่านี้ EA จะไม่เปิดออเดอร์) และใช้ <strong>VELOCE26</strong> ที่ตั้งมาให้พร้อมใช้ได้เลย',
];

---

<Layout title="gdBasket — Adaptive Buy Grid Behavior EA · GOLD M15 | Room" description="EA เทรดทองคำด้วยระบบ Adaptive Grid Behavior ปรับพฤติกรรมตามความผันผวนตลาดเองอัตโนมัติ เสริมด้วย Peak Profit Trail System เก็บกำไรเก่ง ใช้ Common-TP/SL วางจริงที่โบรก ถึงเป้าปิดให้เองแม้ไม่ได้เปิดเครื่อง — VELOCE26 ผ่านพิสูจน์ 6 ปี 8 เดือน 2020–2026 — โหลดฟรี v3.21" ogImage="https://room.intensivetrader.com/gdbasket-og.png" ogImageWidth={1200} ogImageHeight={630}>
<div id="main-content" class="repo-page">
  <div class="repo-shell max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">

    <!-- (a) BREADCRUMB -->
    <nav class="repo-breadcrumb" aria-label="Breadcrumb">
      <a href="/">Room</a><span class="mx-1">/</span>
      <a href="/?filter=ea">EA</a><span class="mx-1">/</span>
      <span>gdBasket</span>
    </nav>

    <!-- (b) REPO HEADER -->
    <header class="repo-header">
      <div class="repo-identity">
        <div class="repo-mark"><img src="/gdbasket-mark.png" alt="gdBasket" /></div>
        <div>
          <div class="repo-name">
            <span>IntensiveTrader</span><i>/</i><strong class="pixel">gdBasket</strong><b>ใช้งานจริง</b>
          </div>
          <p>Adaptive Buy Grid Behavior EA · GOLD M15</p>
        </div>
      </div>
      <div class="repo-version"><span>เวอร์ชัน</span><strong>v3.21</strong></div>
    </header>

    <!-- (c) SUB-NAV TABS -->
    <nav class="repo-tabs" aria-label="เมนู gdBasket">
      <a href="#readme" class="active">
        <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24"><path d="M4 4h16v2H4zm0 5h16v2H4zm0 5h11v2H4z"/></svg>
        โค้ด/ไฟล์
      </a>
      <a href="#how">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13"/></svg>
        วิธีทำงาน
      </a>
      <a href="#features">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 3v4M3 5h4M6 17v4m-2-2h4m5-16l2.286 6.857L21 12l-5.714 2.143L13 21l-2.286-6.857L5 12l5.714-2.143L13 3z"/></svg>
        จุดเด่น
      </a>
      <a href="/ea/gdbasket/changelog">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
        Releases <span>9</span>
      </a>
      <a href={JOURNAL}>Trading Journal</a>
    </nav>

    <div class="repo-columns">

      <!-- LEFT -->
      <div class="repo-main">
        <!-- (d) TOOLBAR -->
        <div class="repo-toolbar">
          <div class="branch-pill">
            <svg class="w-4 h-4 text-gray" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7v10m0-10a2 2 0 100-4 2 2 0 000 4zm0 10a2 2 0 100 4 2 2 0 000-4zm8-6a2 2 0 100-4 2 2 0 000 4zm0 0v1a3 3 0 01-3 3H9"/></svg>
            <strong>v3.21</strong>
          </div>
          <span>
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 7h.01M7 3h5a1.99 1.99 0 011.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.99 1.99 0 013 12V7a4 4 0 014-4z"/></svg>
            <strong>9</strong> เวอร์ชัน
          </span>
          <a href={DL_LATEST} download class="repo-download compact">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"/></svg>
            ดาวน์โหลดฟรี <span>v3.21</span>
          </a>
          <p>ไฟล์รุ่น <strong>v3.21</strong> — ใช้ได้เฉพาะบัญชี <strong>XM</strong> (ทองคำ ไทม์เฟรม M15) ล็อกไว้ในตัว EA</p>
        </div>

        <!-- (e) FILE BROWSER -->
        <section class="file-browser" aria-label="สารบัญ gdBasket">
          <div class="commit-row">
            <img src={`${MAIN}/profile.jpg`} class="commit-avatar" alt="" />
            <strong>IntensiveTrader</strong>
            <p>v3.21 — กฎเท่าทุนทำงานต่อหลัง restart + ตัวซ่อม TP/SL รู้กฎแล้ว · ผลเทสดีขึ้น กำไรรวม +23,544 / ทุน 1,000</p>
            <time>อัปเดตไฟล์ 24 ก.ย. 2026</time>
          </div>
          <div class="file-list">
            {fileRows.map((row) => (
              <a
                href={row.href}
                {...(row.download ? { download: true } : {})}
                class={(row.download || row.accent) ? 'accent' : ''}
              >
                {row.icon === 'doc' && <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24"><path d="M4 4h16v2H4zm0 5h16v2H4zm0 5h11v2H4z"/></svg>}
                {row.icon === 'gear' && <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/></svg>}
                {row.icon === 'modes' && <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6V4m0 2a2 2 0 100 4m0-4a2 2 0 110 4m-6 8a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4m6 6v10m6-2a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4"/></svg>}
                {row.icon === 'star' && <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 3v4M3 5h4M6 17v4m-2-2h4m5-16l2.286 6.857L21 12l-5.714 2.143L13 21l-2.286-6.857L5 12l5.714-2.143L13 3z"/></svg>}
                {row.icon === 'risk' && <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v4m0 4h.01M10.29 3.86l-8.82 14A2 2 0 003.18 21h17.64a2 2 0 001.71-3.14l-8.82-14a2 2 0 00-3.42 0z"/></svg>}
                {row.icon === 'clock' && <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>}
                {row.icon === 'dl' && <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"/></svg>}
                {row.icon === 'journal' && <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/></svg>}
                {row.icon === 'calc' && <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 7h6m-6 4h.01M12 11h.01M15 11h.01M9 15h.01M12 15h.01M15 15h.01M7 3h10a2 2 0 012 2v14a2 2 0 01-2 2H7a2 2 0 01-2-2V5a2 2 0 012-2z"/></svg>}
                <strong>{row.name}</strong>
                <span>{row.note}</span>
                <b>{row.meta}{(row.download || row.accent) ? ' →' : ''}</b>
              </a>
            ))}
          </div>
        </section>

        <!-- (e2) VELOCE26 brand mark -->
        <div style="text-align:center;margin:30px 0 32px;">
          <img src="/veloce-wordmark.png" alt="gdBasket VELOCE26" width="718" height="271" loading="lazy" style="width:100%;max-width:340px;height:auto;" />
        </div>

        <!-- (f) AFFILIATE CARDS — IB / VPS / Copytrade (แบบ A: IB เต็มกว้าง + VPS/Copy คู่ล่าง) -->
        <div class="aff-card card-ib">
          <small>โบรกที่ผมเทรดเอง</small>
          <h2>gdBasket ทำมาเพื่อ XM โดยเฉพาะ</h2>
          <p>EA ตัวนี้ล็อกให้รันเฉพาะ GOLD บน XM — ใครจะเอาไปใช้ต้องมีบัญชี XM ก่อน ผมเทรดโบรกนี้เอง เปิดผ่านลิงก์ผมได้โบนัสเติมเงินด้วย</p>
          <div class="pill-row">
            <span class="pill">Leverage <b>1:1000</b></span>
            <span class="pill">โบนัสเติมเงิน <b>100%</b></span>
            <span class="pill">เปิดบัญชี <b>ฟรี</b></span>
          </div>
          <div class="ref-code referral-strip">
            <span>รหัสผู้แนะนำ:</span>
            <strong>{XM_CODE}</strong>
            <button type="button" data-copy={XM_CODE} class="copy-code">
              <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z"/></svg>
              คัดลอก
            </button>
          </div>
          <a href={XM_AFF} target="_blank" rel="noopener noreferrer sponsored" class="repo-download" style="display:inline-flex;">เปิดบัญชี XM ได้ที่นี่ →</a>
          <p class="disclosure">ลิงก์แนะนำ (affiliate) — ผมได้ค่าตอบแทนถ้าสมัครผ่านลิงก์นี้ · ไม่ใช่คำแนะนำการลงทุน · การเทรดมีความเสี่ยง อาจขาดทุนได้ ตัดสินใจด้วยตัวเอง</p>
        </div>

        <div class="cards-2col">
          <div class="aff-card card-vps">
            <span class="card-icon">
              <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><rect x="3" y="4" width="18" height="6" rx="1.5"/><rect x="3" y="14" width="18" height="6" rx="1.5"/><circle cx="7" cy="7" r=".6" fill="currentColor" stroke="none"/><circle cx="7" cy="17" r=".6" fill="currentColor" stroke="none"/></svg>
            </span>
            <small>VPS ที่ผมใช้เอง</small>
            <h2>รัน EA 24 ชม. ไม่ต้องเปิดคอม</h2>
            <p>เครื่องเซิร์ฟเวอร์ให้ EA รันตลอดเวลา ปิดคอมที่บ้านได้ เน็ตไม่หลุด ไม้ไม่พลาด</p>
            <ul class="aff-list">
              <li>รัน gdBasket ต่อเนื่อง 24 ชม.</li>
              <li>ปิดคอม–เน็ตหลุดก็ไม่กระทบ</li>
              <li>ทดลองฟรี 7 วัน</li>
              <li>เริ่มต้น ฿290/เดือน</li>
            </ul>
            <a href={VPS_AFF} target="_blank" rel="noopener noreferrer sponsored" class="repo-download btn-gold" style="display:inline-flex;">ดู VPS ที่ผมใช้ →</a>
            <p class="disclosure">ลิงก์แนะนำ — ผมได้ค่าตอบแทนถ้าสมัครผ่านลิงก์นี้ · เลือกใช้ VPS เจ้าไหนก็ได้ตามสะดวก</p>
          </div>

          <div class="aff-card card-copy">
            <span class="card-icon">
              <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M17 20h5v-2a4 4 0 00-3-3.87M9 20H4v-2a4 4 0 013-3.87m6-1.13a4 4 0 100-8 4 4 0 000 8zm6 0a4 4 0 10-3-6.65"/><path stroke-linecap="round" stroke-linejoin="round" d="M9 20v-1a5 5 0 0110 0v1"/></svg>
            </span>
            <small>ไม่อยากลง EA เอง</small>
            <h2>Copy ไม้ VELOCE26 อัตโนมัติ</h2>
            <p>ก็อปการเทรดจากพอร์ต gdBasket ของผมตรงๆ ผ่านระบบ Copy Trading ทางการของ XM</p>
            <ul class="aff-list">
              <li>ก็อปไม้อัตโนมัติ ไม่ต้องลง EA</li>
              <li>ระบบทางการของ XM</li>
              <li>เริ่มลงทุนขั้นต่ำ $50</li>
              <li>ทำงานให้ 24 ชม.</li>
            </ul>
            <a href={COPYTRADE_URL} target="_blank" rel="noopener noreferrer sponsored" class="repo-download btn-green" style="display:inline-flex;">ดูรายละเอียด Copy Trade →</a>
            <p class="disclosure">ระบบ Copy Trading ของ XM · ไม่ใช่คำแนะนำการลงทุน · การเทรดมีความเสี่ยง</p>
          </div>
        </div>

        <div class="risk-note">
          <span aria-hidden="true">⚠</span>
          <p><strong>Risk Warning</strong> : การ Copytrade จะไม่ได้การันตีผลลัพธ์ว่าจะได้ผลตอบแทนตามที่ต้องการเสมอไปนะครับ นักลงทุนควรจัดการความเสี่ยงของตนเอง และยอมรับความเสี่ยงที่จะเกิดขึ้น หากสูญเสียเงินทั้งหมดในการลงทุนไป ผู้ลงทุนจะเป็นผู้รับผิดชอบเงินของตัวเอง Master ผู้นำเทรด ไม่ใช่ผู้รับผิดชอบเงินทุนที่สูญเสียไป ฉะนั้นโปรดอ่านและพิจารณาเงื่อนไขความเสี่ยงทั้งหมดเป็นอย่างดีก่อนที่จะตัดสินใจลงทุน</p>
        </div>

        <!-- (g) README -->
        <article id="readme" class="readme-card scroll-mt-20">
          <div class="readme-title">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13"/></svg>
            <strong>README</strong>
          </div>
          <div class="readme-body">
            <img class="readme-chart" src="/gdbasket-chart-v302.png" alt="gdBasket VELOCE26 รันจริงบนกราฟ GOLD M15 — เห็นแผง Live Management ใหม่" />
            <h1 class="pixel">gdBasket</h1>
            <p class="readme-subtitle">Adaptive Buy Grid Behavior EA</p>
            <p>EA เทรดทองคำด้วยระบบ <strong>Adaptive Grid Behavior</strong> ปรับพฤติกรรมการวาง Grid ตามสภาวะความผันผวนของตลาดแบบอัตโนมัติ และเสริมความแกร่งในการทำกำไรด้วยระบบ <strong>Peak Profit Trail System</strong> เก็บกำไรเก่ง เติมไม้ทำกำไรในช่วงตลาดพุ่ง — ใช้ระบบ Safety แบบ <strong>Common-TP/SL</strong> ที่วางจริงไว้ที่โบรก — ถึงเป้าปิดให้เองแม้คุณไม่ได้เปิดเครื่อง</p>
            <p>รุ่นล่าสุดใช้ <strong>VELOCE26</strong> เป็นค่าพร้อมใช้ — เครื่องพีระมิด 2 บ้าน ผ่านการพิสูจน์ยาว 6 ปี 8 เดือน (2020–2026) มาพร้อม<strong>แผงจัดการสด</strong>บนจอที่บอกเหตุผลแบบเรียลไทม์ว่าทำไมมันยังไม่เปิดไม้</p>

            <!-- KPI row เด่น — สรุปผล VELOCE26 -->
            <dl class="kpi-row" aria-label="สรุปผล VELOCE26">
              <div><dt>กำไรสุทธิสะสม (USD)</dt><dd class="positive">+235,281</dd></div>
              <div><dt>สำเร็จ : โดนล้าง (จาก 32 รอบ)</dt><dd>26 : 6</dd></div>
              <div><dt>กำไรจากรอบสำเร็จ</dt><dd class="positive">+261,327</dd></div>
              <div><dt>ทุน 2 เท่ารอด</dt><dd>100%</dd></div>
            </dl>
            <p class="fine-print mb-6">ตัวเลขทั้งหมดคำนวณจากตั้งทุนต่อรอบ (capital) 10,000 → เป้าถอน 20,000/รอบ · ช่วง ม.ค. 2020 – ส.ค. 2026</p>

            <h2 id="features">จุดเด่นของระบบ</h2>
            <p>แกนของ gdBasket คือการรู้ว่าเมื่อไรควรบุก เมื่อไรควรระวัง และใช้จังหวะที่ถูกทางให้คุ้มที่สุด</p>
            <div class="gdb-core-features">
              {features.map((f, i) => (
                <section>
                  <span class="gdb-core-num">0{i + 1}</span>
                  <h3>{f.title}</h3>
                  <p>{f.desc}</p>
                  <span class="gdb-core-tag">{f.tag}</span>
                </section>
              ))}
            </div>

            <h2 id="how">หนึ่งรอบทำงานอย่างไร</h2>
            <ol class="readme-steps">
              {howSteps.map((step, i) => (
                <li><span>{i + 1}</span><p set:html={step} /></li>
              ))}
            </ol>

            <div class="readme-callout">
              <p>gdBasket ทำงานเป็น<strong>รอบๆ</strong> — แต่ละรอบมันตั้งเป้าทำกำไรจาก <strong>Capital</strong> (ทุนต่อรอบ) ที่คุณกรอกไว้ แล้วบริหารทุกอย่างเองเพื่อไปให้ถึงเป้านั้น</p>
              <p>คุณบอกมันแค่ตัวเลขเดียว — <strong>Capital</strong> ที่เหลือมันคิดให้หมด ทั้งขนาดไม้ ระยะซื้อ และจังหวะเก็บกำไรด้วย <strong>PPTS</strong></p>
              <p>เช่นตั้ง Capital <strong>$1,000</strong> → มันจะพยายามปั้นให้ได้กำไร <strong>$1,000</strong> ต่อรอบ และถ้าพลาดจริงๆ ก็ยอมเสียไม่เกิน <strong>$1,000</strong> ต่อรอบเช่นกัน — เพดานได้/เสียชัดตั้งแต่ต้น</p>
            </div>

            <h2>เครื่องยนต์ 2 บ้าน</h2>
            <p>หัวใจของ gdBasket คือระบบเปิดไม้ 2 จังหวะคู่กัน — ทั้ง 2 บ้านใช้วิธี<strong>ทบไม้ไล่ล็อกกำไรแบบเดียวกัน</strong>ด้วย <strong>PPTS</strong> (Peak Profit Trail System) พอราคาวิ่งเข้าทาง มันไม่รีบปิด แต่ไล่ล็อกกำไรตามขึ้นไป ให้กำไรวิ่งต่อได้เต็มที่โดยไม่เสี่ยงคืนกำไรที่ได้มาแล้ว</p>
            <div class="readme-two-col">
              <div>
                <small class="pixel">uptrend</small>
                <h3>บ้านบน</h3>
                <p>ซื้อตอนราคาวิ่งขึ้นตามเทรนด์ ไล่ทบไม้เป็นชั้นๆ ยิ่งราคาวิ่งดีเท่าไหร่ยิ่งเก็บได้มากขึ้นเท่านั้น</p>
              </div>
              <div>
                <small class="pixel">dip-buy</small>
                <h3>บ้านล่าง</h3>
                <p>ซื้อตอนราคาตกลงมา — เครื่องทำเงินหลักของทั้งระบบ เพราะราคาที่ตกแล้วเด้งกลับคือจังหวะทำกำไรได้หนักที่สุด แต่คุมไม่ให้ "อมกอง" ไว้นานเกินไป</p>
              </div>
            </div>

            <h2>จัดการกำไรและความเสียหายเป็นชุดเดียวกัน</h2>
            <div class="gdb-safety-grid">
              <section>
                <b>Common-TP</b>
                <p>ทุกไม้มีเป้ากำไรราคาเดียวกัน วางจริงไว้กับโบรก ถึงเป้าปิดทั้งชุดให้เอง แม้หน้าจอ MT5 ไม่ได้เปิดอยู่</p>
              </section>
              <section>
                <b>Swap Adjust</b>
                <p>ถ้าถือข้ามคืน ระบบขยับเป้ากำไรเพื่อชดเชยค่า Swap ที่ถูกหักจริง ไม่ปล่อยให้ต้นทุนข้ามคืนกินเป้าเดิม</p>
              </section>
              <section>
                <b>Broker-side Stop</b>
                <p>มีจุดหยุดขาดทุนรวมวางจริงไว้กับโบรก เป็นกันชนสุดท้ายเมื่อราคาไหลแรง โปรแกรมดับ หรือเกิด Gap</p>
              </section>
            </div>

            <h2 id="capital">Capital และ Rolling Capital</h2>
            <div class="gdb-capital-grid">
              <div>
                <h3>Capital ต่อรอบ</h3>
                <div class="gdb-formula">$1,000 → เป้ากำไร $1,000</div>
                <p>Capital คือเงินที่มึงยอมเสี่ยงต่อหนึ่งรอบ ไม่ใช่ยอดเงินทั้งหมดในบัญชี ระบบใช้ตัวเลขนี้เป็นฐานคำนวณขนาดไม้ เป้ากำไร และเพดานของรอบ</p>
                <p class="fine-print">ราคา Gap หรือ Slippage อาจทำให้ผลจริงเกินจุดที่ตั้งใจ จึงไม่ใช่ยอดขาดทุนสูงสุดแบบรับประกัน</p>
              </div>
              <div>
                <h3>Rolling Capital</h3>
                <p>เลือกถอนกำไรบางส่วนและนำที่เหลือกลับไปเพิ่มทุนได้ ระบบแยกเงินแต่ละหน้าที่ออกจากกัน และลดขนาดไม้หรือหยุดเปิดรอบใหม่เมื่อเงินจริงไม่พอ</p>
                <div class="gdb-wallets">
                  <div><b>ทุนต่อรอบ</b>ฐานความเสี่ยง</div>
                  <div><b>เงินรอทบ</b>กำไรที่รอเพิ่มทุน</div>
                  <div><b>เงินทำงาน</b>ยอดจริงในบัญชี</div>
                  <div><b>เงินถอนสะสม</b>เงินที่นำออกแล้ว</div>
                </div>
                <a href="/ea/gdbasket/rolling-capital" class="repo-download rolling-cta mt-4" style="display:inline-flex;min-height:40px;padding:0 16px;gap:8px;background:var(--color-secondary);border-color:var(--color-secondary-dark);">ลองวางแผนถอน/ทบทุนจากผลจริงย้อนหลัง →</a>
              </div>
            </div>

            <h2>พร้อมใช้จริงบนพอร์ตจริง</h2>
            <div class="gdb-support-grid">
              <section><b>Standard + Micro</b><p>อ่านขนาดสัญญาจริงและปรับขนาดไม้ให้เข้ากับบัญชีอัตโนมัติ</p></section>
              <section><b>จำสถานะหลังรีสตาร์ต</b><p>ไล่ประวัติจริงกลับมาประกอบรอบเดิม ไม่เริ่มนับใหม่เป็นศูนย์</p></section>
              <section><b>พักหลังชนเพดาน</b><p>เว้นช่วงก่อนเริ่มรอบใหม่ ไม่รีบกลับเข้าเทรดทันทีหลังรอบเสีย</p></section>
              <section><b>Live Management Panel</b><p>เห็นกำไรรอบปัจจุบัน สถานะ และเหตุผลที่ยังไม่เปิดไม้บนกราฟ</p></section>
            </div>

            <h2 id="modes">VELOCE26 · เครื่องยนต์ 2 บ้านที่รู้จังหวะบุกและระวัง</h2>
            <p>VELOCE26 คือสูตรเรือธงของ gdBasket ที่ให้บ้านบนและบ้านล่างทำงานคู่กัน ระบบอ่านโซนราคาและอุณหภูมิตลาดก่อนเลือกวิธีวาง Grid กับขนาด Lot จึงไม่ใช้แรงเท่ากันทุกสถานการณ์</p>
            <section class="engine-readout veloce-panel" id="engine-veloce">
              <div>
                <span class="status-dot"></span><small>ค่าพร้อมใช้ · ผ่านพิสูจน์ 6 ปี 8 เดือน 2020–2026</small>
                <h3 class="pixel">VELOCE26</h3>
                <p class="veloce-lead">เป้าหมายของ VELOCE26 ไม่ใช่แค่ปิดรอบให้ได้ แต่คือ<strong>เร่งเก็บกำไรเมื่อได้ทาง และลดแรงปะทะเมื่อตลาดไม่เป็นใจ</strong></p>
                <div class="veloce-behavior">
                  <div><b>เมื่อตลาดเป็นใจ</b><span>PPTS จะเติมไม้ตามทางและไล่ล็อกกำไรรวมขึ้นไป เพื่อใช้ช่วงที่ราคาวิ่งดีให้คุ้มที่สุด</span></div>
                  <div><b>เมื่อตลาดไม่เป็นใจ</b><span>ระบบจะปรับระยะ Grid และน้ำหนัก Lot ให้เหมาะกับโซนราคา พร้อมกรองจังหวะอันตรายก่อนเปิดรอบใหม่</span></div>
                </div>
                <p>ค่าหลักของ VELOCE26 ถูกคัดจากผลทดสอบย้อนหลัง 6 ปี 8 เดือน (2020–2026) และการเลื่อนช่วงเวลา 25 รูปแบบ ก่อนตรึงมาเป็นค่าพร้อมใช้ใน gdBasket v3.21</p>
              </div>
              <dl>
                <div><dt>กำไรสุทธิย้อนหลัง</dt><dd>+235,281</dd></div>
                <div><dt>รอบสำเร็จ</dt><dd>26</dd></div>
                <div><dt>รอบชนเพดาน</dt><dd class="danger">6</dd></div>
              </dl>
            </section>

            <h2>กว่าจะมาเป็น VELOCE26</h2>
            <ol class="readme-steps">
              {veloceSteps.map((step, i) => (
                <li><span>{i + 1}</span><p set:html={step} /></li>
              ))}
            </ol>

            <h2 id="results">ผลทดสอบย้อนหลัง</h2>
            <p>ตัวเลขด้านล่างมีไว้ให้เห็นพฤติกรรมของ VELOCE26 บนข้อมูลในอดีต ไม่ใช่คำรับประกันผลตอบแทนในอนาคต</p>
            <div id="tbl-veloce" class="proof-table-wrap mb-4">
              <table class="proof-table">
                <thead>
                  <tr><th>สรุปผล — VELOCE26</th><th>จำนวน</th><th>ยอด USD</th></tr>
                </thead>
                <tbody>
                  <tr><td>รอบสำเร็จ (ปั้นครบเป้า)</td><td>26 รอบ</td><td class="positive">+261,327</td></tr>
                  <tr><td>หักรอบโดนล้างพอร์ต</td><td>6 รอบ</td><td class="negative">−26,046</td></tr>
                  <tr class="total"><td>กำไรสุทธิ</td><td></td><td class="positive">+235,281</td></tr>
                </tbody>
              </table>
            </div>
            <p class="fine-print">ช่วงข้อมูล: ม.ค. 2020 – ส.ค. 2026 (6 ปี 8 เดือน) · ทุนต่อรอบ $10,000 · เทสบน GOLD M15 tick จริง ดีเลย์ 100 ms</p>
            <p class="fine-print mb-6">สำเร็จ:ล้าง 26 : 6 จาก 32 รอบ (81%) · กำไรจากรอบสำเร็จ +261,327 · เสียจากล้าง −26,046 · วันต่อรอบสำเร็จ เฉลี่ย 84 / median 31 · floating ลึกสุด −14,324 · basket ลึกสุด 57 ไม้</p>

            <h3>ผลรายปี</h3>
            <div class="proof-table-wrap mb-4">
              <table class="proof-table">
                <thead>
                  <tr><th>ปี</th><th>สำเร็จ : โดนล้าง</th><th>กำไรปี (USD)</th></tr>
                </thead>
                <tbody>
                  <tr><td>2020</td><td>2 : 2</td><td class="positive">+10,305</td></tr>
                  <tr><td>2021</td><td>0 : 0</td><td class="positive">+9,092</td></tr>
                  <tr><td>2022</td><td>1 : 0</td><td class="positive">+3,610</td></tr>
                  <tr><td>2023</td><td>1 : 0</td><td class="positive">+9,584</td></tr>
                  <tr><td>2024</td><td>3 : 1</td><td class="positive">+20,699</td></tr>
                  <tr><td>2025</td><td>8 : 1</td><td class="positive">+80,708</td></tr>
                  <tr><td>2026 (8 เดือน)</td><td>11 : 2</td><td class="positive">+106,084</td></tr>
                </tbody>
              </table>
            </div>
            <p class="fine-print mb-6">ทุนต่อรอบ 10,000 · ปีที่ตลาดนิ่ง (2021–2023) ได้ 0–1 รอบต่อปี — ปีแกว่งแรง (2026) ทำกำไรได้เยอะที่สุด</p>

            <p class="fine-print"><strong>"รอบสำเร็จ"</strong> = รอบที่ปั้นครบเป้า (ในตารางคือ $10,000) โดยไม่ชนเพดานก่อน</p>
            <p class="fine-print mb-6"><strong>"ชนเพดาน" ไม่ได้แปลว่ารอบนั้นขาดทุนล้วน</strong> — ก่อนจะชนเพดาน หลายรอบแบงก์กำไรไปแล้ว กำไรส่วนนี้อยู่ในพอร์ตจริง การชนเพดานแค่ตัดตะกร้าไม้สุดท้ายที่ราคาไหลลงแรง เสียเท่า Capital ที่ตั้งไว้ <strong>ไม่ใช่ล้างพอร์ต</strong> แล้วขึ้นรอบใหม่ต่อ · ตัวเลขกำไรสุทธิหักส่วนที่เสียตอนชนเพดานทั้งหมดแล้ว</p>
            <p class="fine-print mb-6">ตัวเลขหลักทั้งหมดคำนวณจากตั้งทุนต่อรอบ (capital) 10,000 → เป้าถอน 20,000/รอบ, ช่วง ม.ค. 2020 – ส.ค. 2026, GOLD M15 tick จริง ดีเลย์ 100 ms</p>

            <h3>เทียบระดับทุน 1,000 / 10,000</h3>
            <div class="proof-table-wrap mb-4">
              <table class="proof-table">
                <thead>
                  <tr><th>ตัวชี้วัด</th><th>ทุนต่อรอบ $1,000</th><th>ทุนต่อรอบ $10,000</th></tr>
                </thead>
                <tbody>
                  <tr><td>กำไรสุทธิ</td><td class="positive">+23,544</td><td class="positive">+235,281</td></tr>
                  <tr><td>สำเร็จ : โดนล้าง</td><td>26 : 6</td><td>26 : 6</td></tr>
                  <tr><td>กำไรเฉลี่ย/รอบสำเร็จ (โดยประมาณ)</td><td class="positive">≈ +1,005</td><td class="positive">≈ +10,051</td></tr>
                  <tr><td>floating ลึกสุดที่เคยเจอ</td><td class="negative">−1,432</td><td class="negative">−14,324</td></tr>
                  <tr><td>รอด ทุน 1 เท่า</td><td>78%</td><td>78%</td></tr>
                  <tr class="total"><td>รอด ทุน 2 เท่า</td><td>100%</td><td>100%</td></tr>
                </tbody>
              </table>
            </div>
            <p class="fine-print mb-6">ผลทุกตัวเป็นสัดส่วนตรงตามระดับทุน — สัดส่วน 1 : 10 กันไว้ให้เทียบเข้าใจง่าย ก่อนเริ่มควรมีทุน 2 เท่าของทุนต่อรอบ รอด 100% ตลอดชุดข้อมูล</p>
            <p class="fine-print mb-6">ตัวเลขทั้งหมดคำนวณจากตั้งทุนต่อรอบ (capital) 10,000 → เป้าถอน 20,000/รอบ</p>

            <p class="fine-print mb-6"><strong class="text-gray-light">VELOCE26 ทนทุกช่วงเวลาที่ทดสอบ รอด 100%</strong> — รอด 100% จากทุน 2 เท่าของรอบในทุกช่วงที่ทดสอบ ไม่มีช่วงไหนหลุดเกณฑ์ (รายละเอียดเต็มอยู่ในปุ่ม "ดูสถิติเต็ม" ด้านล่าง)</p>

            <div class="readme-callout" style="text-align:center;margin:6px 0 28px;">
              <p style="margin:0 0 6px;color:var(--color-gray-light);font-size:15px;font-weight:700;">อยากรู้ตัวเลขทุกตัวแบบไม่มีย่อ?</p>
              <p style="margin:0 0 16px;">ผลรวม / ความเร็วปั้น / ปั้นติดกัน / ความถี่ล้าง / คุณภาพกำไร / ก้อนขาดทุน / <strong>survival สุ่มจุดเริ่ม</strong> — ครบ 7 กลุ่ม ไม่มีตัดทอน</p>
              <button type="button" class="repo-download" style="display:inline-flex;font-size:13.5px;min-height:46px;padding:0 26px;gap:9px;" onclick="openStatsModal()">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/></svg>
                ดูสถิติเต็มทุกตัว ไม่มีย่อ →
              </button>
            </div>

            <h2 id="risk">ความเสี่ยงที่ต้องรู้ก่อนใช้</h2>
            <section class="gdb-risk-card">
              <div class="gdb-risk-head">
                <b>gdBasket มีระบบคุมความเสียหาย แต่ยังเป็น Buy Grid</b>
                <p>ระบบช่วยจัดการความเสี่ยง ไม่ได้กำจัดความเสี่ยงจากตลาด</p>
              </div>
              <div class="gdb-risk-list">
                <div><b>ซื้ออย่างเดียว และไม่ได้ทำนายทิศทาง</b><p>เปิด Buy ตามเวลาและสภาพตลาด ไม่ได้รอสัญญาณว่าราคาจะขึ้นแน่นอน</p></div>
                <div><b>ราคาลงต่อ ระบบจะสะสมไม้</b><p>อาจเห็นขาดทุนค้างสูงก่อนตลาดเด้งกลับ หรือก่อนถึงจุดหยุดรวม</p></div>
                <div><b>ไม่มี Stop Loss แยกรายไม้</b><p>ทุกไม้ถูกดูแลเป็นกองเดียว และใช้จุดหยุดรวมของทั้งรอบ</p></div>
                <div><b>หนึ่งรอบอาจเสียใกล้เคียง Capital</b><p>Gap, Slippage หรือสภาพคล่องบางช่วง อาจทำให้ผลจริงเกินระดับที่ตั้งใจ</p></div>
                <div><b>ผลย้อนหลังไม่รับประกันอนาคต</b><p>Spread, Swap, Slippage และตลาดจริงอาจทำให้ผลต่างจากการทดสอบ</p></div>
                <div><b>Rolling Capital ทำให้เงินเสี่ยงโตตามทุน</b><p>เมื่อ Capital โต ขนาด Lot และจำนวนเงินที่ยอมเสี่ยงต่อรอบจะโตด้วย</p></div>
                <div><b>ใช้เฉพาะ XM GOLD M15</b><p>ค่าของระบบออกแบบกับสัญญา XM ไม่ควรนำไปใช้กับโบรกหรือสินทรัพย์อื่น</p></div>
                <div><b>EA ต้องทำงานต่อเนื่องเพื่อจัดการไม้ใหม่</b><p>TP/SL ที่โบรกยังอยู่ แต่การเปิดและปรับแผนใหม่ต้องให้ EA ทำงาน</p></div>
              </div>
              <div class="gdb-risk-rule"><strong>หลักที่ใช้:</strong> มีทุนเท่าไร เอามาหาร 3 แล้วใช้ผลลัพธ์เป็น Capital เพื่อไม่ให้รอบเดียวกินเงินทั้งบัญชี</div>
            </section>

            <h2 id="install">วิธีติดตั้ง</h2>
            <a href={XM_AFF} target="_blank" rel="noopener noreferrer sponsored" class="flex items-start gap-3 bg-secondary/5 border border-secondary/30 rounded-lg p-3.5 mb-4 hover:border-secondary/60 transition-colors">
              <span class="shrink-0 w-7 h-7 rounded-lg bg-secondary text-[#1a1205] font-black grid place-items-center text-sm">0</span>
              <span class="text-sm text-gray-light pt-0.5">ยังไม่มีบัญชี XM? <span class="text-secondary font-semibold">เปิดที่นี่</span> (รหัสผู้แนะนำ <span class="text-secondary font-semibold">KWASATI</span>) — gdBasket ใช้กับ XM เท่านั้น</span>
            </a>
            <div class="install-list">
              {installSteps.map((step, i) => (
                <div><span>{i + 1}</span><p set:html={step} /></div>
              ))}
            </div>
            <p class="text-xs text-gray-dark mt-4">gdBasket ล็อกให้รันเฉพาะ GOLD M15 บนโบรก XM เท่านั้น</p>

            <div class="risk-note">
              <span aria-hidden="true">⚠</span>
              <p><strong>อัปเกรดจากรุ่นเก่า</strong> — ลาก EA ขึ้นชาร์ตครั้งแรกให้กด <strong>Reset</strong> ที่หน้า Inputs หนึ่งครั้ง ให้ค่าตั้งต้นมาถูกต้อง</p>
            </div>

            <div class="mini-row">
              <div class="mini-card card-ib">
                <span class="mini-icon">
                  <svg fill="none" stroke="currentColor" stroke-width="1.6" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M2.25 8.25h19.5M2.25 9h19.5m-16.5 5.25h6m-6 2.25h3M3.75 4.5h16.5a1.5 1.5 0 011.5 1.5v12a1.5 1.5 0 01-1.5 1.5H3.75a1.5 1.5 0 01-1.5-1.5v-12a1.5 1.5 0 011.5-1.5z"/></svg>
                </span>
                <h4>เปิดบัญชี XM</h4>
                <p>โบรกที่ผมเทรดเอง — gdBasket ใช้ได้เฉพาะ XM</p>
                <p class="mini-code">รหัส: <strong>{XM_CODE}</strong></p>
                <a href={XM_AFF} target="_blank" rel="noopener noreferrer sponsored" class="mini-cta">เปิดบัญชี →</a>
              </div>

              <div class="mini-card card-vps">
                <span class="mini-icon">
                  <svg fill="none" stroke="currentColor" stroke-width="1.6" viewBox="0 0 24 24"><rect x="3" y="4" width="18" height="6" rx="1.5"/><rect x="3" y="14" width="18" height="6" rx="1.5"/><circle cx="7" cy="7" r=".6" fill="currentColor" stroke="none"/><circle cx="7" cy="17" r=".6" fill="currentColor" stroke="none"/></svg>
                </span>
                <h4>VPS รัน EA 24 ชม.</h4>
                <p>ปิดคอมได้ ไม่ต้องเปิดทิ้งไว้ทั้งวัน</p>
                <a href={VPS_AFF} target="_blank" rel="noopener noreferrer sponsored" class="mini-cta">ดู VPS →</a>
              </div>

              <div class="mini-card card-copy">
                <span class="mini-icon">
                  <svg fill="none" stroke="currentColor" stroke-width="1.6" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M17 20h5v-2a4 4 0 00-3-3.87M9 20H4v-2a4 4 0 013-3.87m6-1.13a4 4 0 100-8 4 4 0 000 8zm6 0a4 4 0 10-3-6.65"/><path stroke-linecap="round" stroke-linejoin="round" d="M9 20v-1a5 5 0 0110 0v1"/></svg>
                </span>
                <h4>Copy เทรดอัตโนมัติ</h4>
                <p>ไม่อยากลง EA เอง ก็อปไม้จาก VELOCE26 ได้เลย</p>
                <a href={COPYTRADE_URL} target="_blank" rel="noopener noreferrer sponsored" class="mini-cta">ดูรายละเอียด →</a>
              </div>
            </div>
            <div class="risk-note">
              <span aria-hidden="true">⚠</span>
              <p><strong>Risk Warning</strong> : การ Copytrade จะไม่ได้การันตีผลลัพธ์ว่าจะได้ผลตอบแทนตามที่ต้องการเสมอไปนะครับ นักลงทุนควรจัดการความเสี่ยงของตนเอง และยอมรับความเสี่ยงที่จะเกิดขึ้น หากสูญเสียเงินทั้งหมดในการลงทุนไป ผู้ลงทุนจะเป็นผู้รับผิดชอบเงินของตัวเอง Master ผู้นำเทรด ไม่ใช่ผู้รับผิดชอบเงินทุนที่สูญเสียไป ฉะนั้นโปรดอ่านและพิจารณาเงื่อนไขความเสี่ยงทั้งหมดเป็นอย่างดีก่อนที่จะตัดสินใจลงทุน</p>
            </div>
          </div>
        </article>

        <!-- (h) CHANGELOG — id="changelog" (ไม่ใช่ #release) -->
        <section id="changelog" class="release-card scroll-mt-20">
          <div>
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
            <strong>BIG UPDATE! VELOCE26 มาแล้ว</strong>
          </div>
          <p><strong>ปรับปรุงกลไกการเทรดใหม่</strong> — ให้สอดคล้องกับความผันผวนของปี 2026 · <strong>พิสูจน์แล้ว</strong> — ล้างพอร์ต (SO) เหลือ 2 จาก 9 ในปี 2026 · <strong>Robustness Test ผ่าน</strong> — ผลปีย้อนหลัง 2020–2025 ไม่เสียไป และยังทำกำไรได้ดีขึ้น · <strong>โปรไฟล์ใหม่ VELOCE26</strong> — ช่อง Mode เหลือตัวเดียว หลังอัปตรวจให้ตรง · <strong>อัปทับได้เลย</strong> — ไม่ต้อง Reset input</p>
          <a href="/ea/gdbasket/changelog" class="text-primary text-sm font-semibold hover:underline px-4 pb-4 inline-block">ดูประวัติทุกเวอร์ชัน →</a>
        </section>
      </div>

      <!-- RIGHT SIDEBAR -->
      <aside class="repo-sidebar" id="repo-sidebar">
        <!-- About -->
        <section>
          <h2>About</h2>
          <p>Adaptive Buy Grid Behavior EA — ทยอยซื้อเป็นชุด ปิดรับกำไรทั้งชุด ใช้จริงบนพอร์ตจริง</p>
          <dl>
            <div><dt>ตลาด</dt><dd>GOLD (XAUUSD)</dd></div>
            <div><dt>Timeframe</dt><dd>M15</dd></div>
            <div><dt>Broker</dt><dd><a href={XM_AFF} target="_blank" rel="noopener noreferrer sponsored" class="text-secondary font-semibold hover:underline">XM · เปิดบัญชี →</a></dd></div>
            <div><dt>ราคา</dt><dd class="positive">ฟรี</dd></div>
            <div><dt>สถานะ</dt><dd class="positive">ใช้งานจริง</dd></div>
          </dl>
        </section>

        <!-- Trading Journal -->
        <section>
          <h2>Trading Journal <small>· ผลเทรดจริง</small></h2>
          <!-- toggle รวม/ต่อพอร์ต — ปุ่มสร้างจาก account_login จริงใน client script (label จาก accounts.display_name, fallback เลขบัญชี); ซ่อนไว้จนมีมากกว่า 1 พอร์ต -->
          <div id="tj-toggle" class="hidden gap-1 mb-3 bg-dark border border-dark-border rounded-lg p-1 text-[11px] font-semibold"></div>
          <div class="journal-box">
            <div><span>กำไรสะสม</span><strong id="tj-pnl">—</strong></div>
            <div><span>Win rate</span><strong id="tj-wr">—</strong></div>
            <div><span>จำนวนรอบ</span><strong id="tj-ct">—</strong></div>
            <a href={JOURNAL}>ดูประวัติเทรดทั้งหมด →</a>
          </div>
        </section>

        <!-- Myfxbook — id="mfb-list" = ที่ client script (ท้ายไฟล์) clone การ์ดเพิ่มเวลามีพอร์ตอื่นผูกเข้า myfxbook -->
        <section>
          <h2>Myfxbook <small>· ผลตรวจสอบจริง</small></h2>
          <div id="mfb-list" class="flex flex-col gap-3">
            {mfbCards.map((c) => (
              <div class="account-box" data-mfb-id={c.id}>
                <div>
                  <span class="status-dot"></span>
                  <strong class="mfb-label">{c.name} · {c.label}</strong>
                  <span>{c.gain != null ? 'LIVE' : 'ใหม่'}</span>
                </div>
                <dl>
                  <div><dt>Gain</dt><dd class={`mfb-gain ${c.gain != null ? 'positive' : ''}`}>{c.gain != null ? `+${c.gain}%` : '—'}</dd></div>
                  <div><dt>กำไรรวม</dt><dd class={`mfb-absgain ${c.absGain != null ? 'positive' : ''}`}>{c.absGain != null ? `+${c.absGain}%` : '—'}</dd></div>
                  <div><dt>ต่อเดือน</dt><dd class="mfb-monthly">{c.monthly != null ? `${c.monthly}%` : '—'}</dd></div>
                  <div><dt>ติดลบสูงสุด</dt><dd class="mfb-dd">{c.drawdown != null ? `${c.drawdown}%` : '—'}</dd></div>
                  <div><dt>ยอดเงิน</dt><dd class="mfb-balance">{c.balance != null ? `$${c.balance}` : '—'}</dd></div>
                </dl>
                {c.newNote && <p class="mfb-new-note">{c.newNote}</p>}
                <a href={c.url ?? '#'} target="_blank" rel="noopener noreferrer" class="mfb-link">ดูใน Myfxbook →</a>
              </div>
            ))}
          </div>
        </section>

        <!-- Releases — widget สไตล์ GitHub: การ์ดรุ่นล่าสุด + ลิงก์หน้าเต็ม -->
        <section id="releases-widget">
          <div class="flex items-center justify-between mb-3">
            <a href="/ea/gdbasket/changelog" class="hover:text-primary transition-colors"><h2 class="!mb-0">Releases</h2></a>
            <span class="bg-dark-border text-gray-light text-[10px] px-1.5 py-0.5 rounded-full font-semibold">{releases.length}</span>
          </div>
          <a href="/ea/gdbasket/changelog" class="block rounded-xl border border-dark-border bg-dark-card p-4 hover:border-primary transition-colors">
            <div class="flex items-center gap-2 flex-wrap">
              <svg class="w-4 h-4 text-green shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 7h.01M7 3h5a1.99 1.99 0 011.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.99 1.99 0 013 12V7a4 4 0 014-4z"/></svg>
              <span class="pixel text-gray-light text-[11px]">gdBasket {releases[0].version}</span>
              <span class="bg-green/10 text-green text-[10px] px-1.5 py-0.5 rounded-full font-semibold">ล่าสุด</span>
            </div>
            <p class="text-xs text-gray-dark mt-1.5 ml-6">{releases[0].dateLabel}</p>
          </a>
          <a href="/ea/gdbasket/changelog" class="inline-block text-primary hover:underline text-sm mt-3">+ {releases.length - 1} releases ทั้งหมด →</a>
        </section>

        <!-- Quick links -->
        <section>
          <h2>Quick links</h2>
          <nav class="sidebar-links">
            <a href="#readme">
              <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13"/></svg>
              README
            </a>
            <a href="#features">
              <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 3v4M3 5h4M6 17v4m-2-2h4m5-16l2.286 6.857L21 12l-5.714 2.143L13 21l-2.286-6.857L5 12l5.714-2.143L13 3z"/></svg>
              จุดเด่นของระบบ
            </a>
            <a href="#modes">
              <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6V4m0 2a2 2 0 100 4m0-4a2 2 0 110 4m-6 8a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4m6 6v10m6-2a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4"/></svg>
              เครื่องยนต์ VELOCE26
            </a>
            <a href="#risk">
              <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v4m0 4h.01M10.29 3.86l-8.82 14A2 2 0 003.18 21h17.64a2 2 0 001.71-3.14l-8.82-14a2 2 0 00-3.42 0z"/></svg>
              ความเสี่ยงที่ต้องรู้
            </a>
            <a href="#results">
              <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
              ผลทดสอบ VELOCE26
            </a>
            <a href="#install">
              <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/></svg>
              วิธีติดตั้ง
            </a>
            <a href="#changelog">
              <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
              Releases
            </a>
            <a href="/ea/gdbasket/rolling-capital">
              <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 7h6m-6 4h.01M12 11h.01M15 11h.01M9 15h.01M12 15h.01M15 15h.01M7 3h10a2 2 0 012 2v14a2 2 0 01-2 2H7a2 2 0 01-2-2V5a2 2 0 012-2z"/></svg>
              วางแผนเงิน Rolling Capital
            </a>
          </nav>
        </section>

        <!-- Topics -->
        <section>
          <h2>Topics</h2>
          <div class="topic-list"><span>mt5</span><span>gold</span><span>grid-ea</span><span>risk-control</span><span>xm</span></div>
        </section>

        <!-- Contributor + bug report -->
        <section>
          <h2>ผู้พัฒนา</h2>
          <div class="flex items-center gap-2 mb-4">
            <img src={`${MAIN}/profile.jpg`} class="w-8 h-8 rounded-full bg-dark-border" alt="" />
            <div><div class="text-sm text-gray-light">IntensiveTrader</div><div class="text-xs text-gray-dark">Kittitat Wasati</div></div>
          </div>
          <a href="https://www.facebook.com/IntensiveTraderArt" target="_blank" rel="noopener noreferrer" class="inline-flex items-center gap-2 border border-dark-border text-gray-light hover:border-primary px-4 py-2 rounded-lg text-sm transition-colors">
            <svg class="w-4 h-4 text-primary" fill="currentColor" viewBox="0 0 24 24"><path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/></svg>
            แจ้ง Bug / ติดต่อ
          </a>
        </section>
      </aside>

    </div>
  </div>
</div>

<!-- popup "ดูสถิติเต็ม" — ผลทดสอบย้อนหลัง 2020–2026 + survival -->
<div id="stats-modal-overlay" class="stats-modal-overlay" onclick="if(event.target===this) closeStatsModal()">
  <div class="stats-modal" role="dialog" aria-modal="true" aria-labelledby="stats-modal-title">
    <div class="stats-modal-head">
      <div>
        <span class="eyebrow pixel" style="font-size:8px;">VELOCE26 · FULL REPORT</span>
        <h2 id="stats-modal-title">สถิติเต็ม — ผลทดสอบย้อนหลัง 2020–2026</h2>
        <p>สรุปจาก Strategy Tester ทุน 10,000 · ม.ค. 2020 – ส.ค. 2026</p>
      </div>
      <button type="button" class="stats-modal-close" onclick="closeStatsModal()" aria-label="ปิด">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"/></svg>
      </button>
    </div>
    <div class="stats-modal-body">

      <h3>ผลรวม — ชุดทุน 10,000</h3>
      <div class="proof-table-wrap mb-4">
        <table class="proof-table">
          <thead><tr><th>สำเร็จ</th><th>โดนล้าง (SO)</th><th>จาก 32 รอบ</th><th>สำเร็จอัตรา</th><th>กำไรสุทธิ</th></tr></thead>
          <tbody><tr><td>26</td><td>6</td><td>32 รอบ</td><td>81%</td><td class="positive">+235,281</td></tr></tbody>
        </table>
      </div>
      <p>กำไรจากรอบสำเร็จ <strong class="positive">+261,327</strong> · เสียจากล้าง <strong class="negative">−26,046</strong> · ช่วง ม.ค. 2020 – ส.ค. 2026 (6 ปี 8 เดือน) · GOLD M15 tick จริง ดีเลย์ 100 ms</p>

      <h3>ความเร็วปั้นสำเร็จ (วันต่อรอบสำเร็จ)</h3>
      <div class="proof-table-wrap mb-4">
        <table class="proof-table">
          <thead><tr><th>เฉลี่ย</th><th>กลาง (median)</th></tr></thead>
          <tbody><tr><td>84 วัน</td><td>31 วัน</td></tr></tbody>
        </table>
      </div>

      <h3>ผลรายปี (สำเร็จ : โดนล้าง · กำไรปี)</h3>
      <div class="proof-table-wrap mb-4">
        <table class="proof-table">
          <thead><tr><th>ปี</th><th>สำเร็จ : โดนล้าง</th><th>กำไรปี (USD)</th></tr></thead>
          <tbody>
            <tr><td>2020</td><td>2 : 2</td><td class="positive">+10,305</td></tr>
            <tr><td>2021</td><td>0 : 0</td><td class="positive">+9,092</td></tr>
            <tr><td>2022</td><td>1 : 0</td><td class="positive">+3,610</td></tr>
            <tr><td>2023</td><td>1 : 0</td><td class="positive">+9,584</td></tr>
            <tr><td>2024</td><td>3 : 1</td><td class="positive">+20,699</td></tr>
            <tr><td>2025</td><td>8 : 1</td><td class="positive">+80,708</td></tr>
            <tr><td>2026 (8 เดือน)</td><td>11 : 2</td><td class="positive">+106,084</td></tr>
          </tbody>
        </table>
      </div>
      <p class="fine-print mb-6">รวมสำเร็จ 26 · รวมโดนล้าง 6 — ตรงกับผลรวมข้างบน · ปีที่ตลาดนิ่ง (2021–2023) ได้ 0–1 รอบต่อปี ปีแกว่งแรง (2026) ทำกำไรได้เยอะที่สุด</p>

      <h3>คุณภาพกำไร</h3>
      <div class="proof-table-wrap mb-4">
        <table class="proof-table">
          <thead><tr><th>กำไรจากรอบสำเร็จ</th><th>เสียจากล้าง (SO)</th></tr></thead>
          <tbody><tr><td class="positive">+261,327</td><td class="negative">−26,046</td></tr></tbody>
        </table>
      </div>
      <p class="fine-print mb-6">กำไรจากรอบสำเร็จ +261,327 เทียบกับเสียจากล้าง −26,046 — กำไรที่ได้จากรอบที่ปั้นครบเป้าเยอะกว่ารวมที่เสียจากล้างชัดเจน · ถือว่าคุ้มมาก</p>

      <h3>ก้อนขาดทุน / ความลึกที่เคยเจอ</h3>
      <div class="proof-table-wrap mb-4">
        <table class="proof-table">
          <thead><tr><th>floating ลึกสุดที่เคยเจอ</th><th>basket ลึกสุด (ไม้เปิดพร้อมกันมากสุด)</th></tr></thead>
          <tbody><tr><td class="negative">−14,324</td><td>57 ไม้</td></tr></tbody>
        </table>
      </div>

      <div class="readme-callout" style="border-left-color:var(--color-green);margin-top:30px;">
        <h3 style="margin:0 0 4px;border:0;padding:0;">กลุ่ม 7 — การทดสอบ "สุ่มจุดเริ่ม" (Survival)</h3>
        <p style="margin:0 0 12px;"><strong>นี่คือการทดสอบสุ่มจุดเริ่ม</strong> — จำลองเอา VELOCE26 ไปเริ่มรันที่จุดไหนของเส้นเวลาก็ได้ (สุ่มจังหวะ ไม่เลือกจังหวะที่ดูดี) แล้วดูว่าทุนระดับที่ใช้จริงจะรอดจนปั้นสำเร็จก่อนหรือตายก่อน (ทุนหมดกลางทาง)</p>
        <div class="proof-table-wrap mb-4">
          <table class="proof-table">
            <thead><tr><th>ทุนที่ใช้</th><th>รอด %</th></tr></thead>
            <tbody>
              <tr><td>1 เท่าของทุนต่อรอบ (10,000)</td><td class="negative">78%</td></tr>
              <tr><td>2 เท่าของทุนต่อรอบ (20,000)</td><td class="positive">100%</td></tr>
            </tbody>
          </table>
        </div>
        <p style="margin:0;">ถ้ากันทุนไว้ <strong class="positive">2 เท่าของทุนต่อรอบ (20,000) ขึ้นไป ไม่เคยตายเลยในข้อมูลทั้งหมด</strong> ไม่ว่าจะเริ่มจังหวะไหน — แนะนำกันทุนสำรองอย่างน้อย 2 เท่าของทุนต่อรอบ</p>
      </div>

    </div>
  </div>
</div>

<style>
  .install-path {
    display: inline-block;
    background: var(--color-dark);
    padding: 0.15rem 0.5rem;
    border-radius: 4px;
    font-family: 'Courier New', monospace;
    font-size: 0.78rem;
    color: var(--color-secondary);
  }

  /* KPI row VELOCE26 — adapt จาก .engine-readout dl (4 คอลัมน์ ยืนเดี่ยวได้) */
  .kpi-row { margin: 6px 0 27px; display: grid; grid-template-columns: repeat(4, 1fr); border-top: 1px solid var(--color-dark-border); border-left: 1px solid var(--color-dark-border); border-radius: 8px; overflow: hidden; }
  .kpi-row > div { min-width: 0; display: flex; flex-direction: column-reverse; justify-content: center; padding: 16px 10px; border-right: 1px solid var(--color-dark-border); border-bottom: 1px solid var(--color-dark-border); text-align: center; background: var(--color-dark-card); }
  .kpi-row dt { color: var(--color-gray-dark); font-size: 9.5px; line-height: 1.4; margin-top: 5px; }
  .kpi-row dd { margin: 0; color: var(--color-green); font-size: 18px; font-weight: 800; }
  .kpi-row dd.danger { color: var(--color-primary-light); }

  .gdb-core-features { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 10px; margin: 14px 0 28px; }
  .gdb-core-features section, .gdb-safety-grid section, .gdb-support-grid section { padding: 16px; border: 1px solid var(--color-dark-border); border-radius: 8px; background: var(--color-dark-card); }
  .gdb-core-num { display: inline-grid; place-items: center; width: 25px; height: 25px; color: var(--color-primary-light); background: rgba(220,38,38,.09); border: 1px solid rgba(220,38,38,.22); border-radius: 6px; font-size: 9px; font-weight: 800; }
  .gdb-core-features h3 { margin: 12px 0 6px; color: var(--color-gray-light); font-size: 13px; }
  .gdb-core-features p { margin: 0; color: var(--color-gray); font-size: 11px; line-height: 1.65; }
  .gdb-core-tag { display: inline-block; margin-top: 12px; padding: 4px 7px; color: var(--color-secondary); background: rgba(245,158,11,.07); border: 1px solid rgba(245,158,11,.2); border-radius: 999px; font-size: 8px; font-weight: 700; }

  .gdb-safety-grid { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 10px; margin: 14px 0 28px; }
  .gdb-safety-grid b, .gdb-support-grid b { display: block; color: var(--color-gray-light); font-size: 12px; }
  .gdb-safety-grid p, .gdb-support-grid p { margin: 7px 0 0; color: var(--color-gray); font-size: 10.5px; line-height: 1.65; }

  .gdb-capital-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); overflow: hidden; margin: 14px 0 28px; border: 1px solid var(--color-dark-border); border-radius: 8px; }
  .gdb-capital-grid > div { padding: 19px; background: var(--color-dark-card); }
  .gdb-capital-grid > div + div { border-left: 1px solid var(--color-dark-border); background: rgba(255,255,255,.012); }
  .gdb-capital-grid h3 { margin: 0; color: var(--color-gray-light); font-size: 14px; }
  .gdb-capital-grid p { margin: 9px 0 0; color: var(--color-gray); font-size: 11px; line-height: 1.7; }
  .gdb-formula { margin: 12px 0; color: var(--color-secondary); font-size: 18px; font-weight: 800; }
  .gdb-wallets { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 7px; margin-top: 12px; }
  .gdb-wallets > div { padding: 8px; border: 1px solid var(--color-dark-border); border-radius: 6px; color: var(--color-gray-dark); font-size: 9px; }
  .gdb-wallets b { display: block; margin-bottom: 3px; color: var(--color-gray-light); font-size: 10px; }

  .gdb-support-grid { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 8px; margin: 14px 0 28px; }
  .gdb-support-grid section { padding: 13px; }
  .gdb-support-grid p { font-size: 9.5px; }

  .referral-strip { width: 100%; min-height: 48px; gap: 10px; padding: 10px 12px; }
  .referral-strip strong { min-width: 92px; padding: 5px 10px; text-align: center; font-size: 12px; }
  .referral-strip .copy-code { min-height: 30px; padding: 6px 10px; }

  .veloce-panel { margin: 14px 0 28px; }
  .veloce-panel .veloce-lead { color: var(--color-gray-light); font-size: 13px; line-height: 1.7; }
  .veloce-panel .veloce-lead strong { color: var(--color-gray-light); }
  .veloce-behavior { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 1px; overflow: hidden; margin: 14px 0; border: 1px solid var(--color-dark-border); border-radius: 6px; background: var(--color-dark-border); }
  .veloce-behavior > div { padding: 13px; background: var(--color-dark); }
  .veloce-behavior b, .veloce-behavior span { display: block; }
  .veloce-behavior b { color: var(--color-secondary); font-size: 11px; }
  .veloce-behavior span { margin-top: 4px; color: var(--color-gray); font-size: 11px; line-height: 1.65; }

  .gdb-risk-card { overflow: hidden; margin: 12px 0 28px; border: 1px solid var(--color-dark-border); border-radius: 8px; background: var(--color-dark-card); }
  .gdb-risk-head { padding: 16px 18px; border-bottom: 1px solid var(--color-dark-border); background: rgba(255,255,255,.012); }
  .gdb-risk-head b { color: var(--color-gray-light); font-size: 14px; }
  .gdb-risk-head b::before { content: '⚠'; margin-right: 8px; color: var(--color-secondary); font-size: 12px; }
  .gdb-risk-head p { margin: 3px 0 0; color: var(--color-gray-dark); font-size: 10px; }
  .gdb-risk-list { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 0 24px; padding: 10px 18px 16px; }
  .gdb-risk-list > div { padding: 11px 0; border-bottom: 1px solid rgba(255,255,255,.055); }
  .gdb-risk-list b { display: block; color: var(--color-gray-light); font-size: 11px; }
  .gdb-risk-list p { margin: 3px 0 0; color: var(--color-gray-dark); font-size: 10px; line-height: 1.6; }
  .gdb-risk-rule { margin: 0 18px 18px; padding: 11px 13px; border-left: 2px solid rgba(245,158,11,.6); border-radius: 6px; background: rgba(245,158,11,.035); color: var(--color-gray); font-size: 10.5px; line-height: 1.65; }
  .gdb-risk-rule strong { color: var(--color-secondary); }

  @media (max-width: 600px) {
    .kpi-row { grid-template-columns: repeat(2, 1fr) !important; }
    .referral-strip { align-items: center; }
    .referral-strip .copy-code { margin-left: auto; }
    .gdb-core-features, .gdb-safety-grid, .gdb-support-grid, .veloce-behavior, .gdb-risk-list { grid-template-columns: 1fr; }
    .gdb-capital-grid { grid-template-columns: 1fr; }
    .gdb-capital-grid > div + div { border-top: 1px solid var(--color-dark-border); border-left: 0; }
  }

  /* popup "ดูสถิติเต็ม" — overlay ใหม่ (backdrop-blur เดียวกับ .glass-dark) + กล่องเนื้อหายืมค่าจาก .readme-card/.course-preview */
  .stats-modal-overlay { position: fixed; inset: 0; z-index: 300; display: none; align-items: center; justify-content: center; padding: 20px; background: rgba(0,0,0,.75); backdrop-filter: blur(6px); }
  .stats-modal-overlay.open { display: flex; }
  .stats-modal { width: 100%; max-width: 880px; max-height: 88vh; overflow-y: auto; background: var(--color-dark-card); border: 1px solid var(--color-dark-border); border-radius: 9px; box-shadow: 8px 8px 0 rgba(0,0,0,.18); scrollbar-width: thin; scrollbar-color: var(--color-dark-border) transparent; }
  .stats-modal::-webkit-scrollbar { width: 8px; }
  .stats-modal::-webkit-scrollbar-track { background: transparent; }
  .stats-modal::-webkit-scrollbar-thumb { background: var(--color-dark-border); border-radius: 999px; }
  .stats-modal::-webkit-scrollbar-thumb:hover { background: var(--color-primary); }
  .stats-modal-head { position: sticky; top: 0; z-index: 2; display: flex; align-items: flex-start; justify-content: space-between; gap: 16px; padding: 18px clamp(16px, 3vw, 28px); background: var(--color-dark-card); border-bottom: 1px solid var(--color-dark-border); }
  .stats-modal-head span.eyebrow { display: block; color: var(--color-primary-light); font-size: 10px; letter-spacing: .16em; }
  .stats-modal-head h2 { margin: 5px 0 0; font-size: 19px; font-weight: 700; color: var(--color-gray-light); border: 0; padding: 0; }
  .stats-modal-head p { margin: 4px 0 0; color: var(--color-gray-dark); font-size: 11px; }
  .stats-modal-close { width: 44px; height: 44px; flex: 0 0 auto; display: grid; place-items: center; color: var(--color-gray-light); background: rgba(220,38,38,.08); border: 1px solid rgba(220,38,38,.45); border-radius: 7px; cursor: pointer; }
  .stats-modal-close svg { width: 19px; height: 19px; }
  .stats-modal-close:hover { color: var(--color-gray-light); border-color: var(--color-primary); }
  .stats-modal-body { padding: 20px clamp(16px, 3vw, 28px) 30px; color: var(--color-gray); font-size: 13px; line-height: 1.75; }
  .stats-modal-body h3 { margin: 30px 0 12px; padding-bottom: 7px; color: var(--color-gray-light); border-bottom: 1px solid var(--color-dark-border); font-size: 14px; }
  .stats-modal-body h3:first-child { margin-top: 0; }
  .stats-modal-body p { margin: 0 0 12px; }
  .stats-modal-body strong { color: var(--color-gray-light); }
  @media (max-width: 600px) {
    .stats-modal-overlay { padding: 0; }
    .stats-modal { max-width: 100%; max-height: 100vh; height: 100vh; border-radius: 0; }
    .stats-modal-head { gap: 10px; padding: 14px 16px; }
    .stats-modal-close { width: 48px; height: 48px; border-radius: 9px; }
    .readme-callout .rolling-cta { display: flex !important; width: 100%; min-height: 48px; justify-content: center; padding: 11px 16px !important; line-height: 1.45; text-align: center; white-space: normal; }
    .rolling-cta svg { flex: 0 0 auto; }
  }
</style>

<script>
  // popup "ดูสถิติเต็ม" — เปิด/ปิด (ปุ่ม X / คลิก overlay / กด Esc / back มือถือ)
  var statsModalHasHistoryEntry = false;
  function openStatsModal(){
    var overlay = document.getElementById('stats-modal-overlay');
    if (!overlay || overlay.classList.contains('open')) return;
    overlay.classList.add('open');
    document.body.style.overflow = 'hidden';
    history.pushState({ ...(history.state || {}), gdbasketStatsModal: true }, '', window.location.href);
    statsModalHasHistoryEntry = true;
  }
  function closeStatsModal(fromHistory){
    var overlay = document.getElementById('stats-modal-overlay');
    if (!overlay) return;
    overlay.classList.remove('open');
    document.body.style.overflow = '';
    if (statsModalHasHistoryEntry && !fromHistory) {
      statsModalHasHistoryEntry = false;
      history.back();
      return;
    }
    statsModalHasHistoryEntry = false;
  }
  document.addEventListener('keydown', function (e) {
    if (e.key === 'Escape') closeStatsModal();
  });
  window.addEventListener('popstate', function () {
    var overlay = document.getElementById('stats-modal-overlay');
    if (overlay && overlay.classList.contains('open')) closeStatsModal(true);
  });
  window.openStatsModal = openStatsModal;
  window.closeStatsModal = closeStatsModal;

  // fix sidebar sticky — top คำนวณจากความสูงจริง แทนค่าตายตัว
  (function () {
    var sidebar = document.getElementById('repo-sidebar');
    if (!sidebar) return;
    var mobileQuery = window.matchMedia('(max-width: 820px)');

    function reflow() {
      if (mobileQuery.matches) {
        // ≤820px = position:static ใน CSS อยู่แล้ว — ไม่ต้องยุ่ง var
        return;
      }
      var margin = 24; // เผื่อขอบล่างจอ
      var baseTop = 96; // ค่าตั้งต้นเดิมของ .repo-sidebar
      var h = sidebar.offsetHeight;
      var vh = window.innerHeight;
      var top = Math.min(baseTop, vh - h - margin);
      sidebar.style.setProperty('--sb-top', top + 'px');
    }

    window.addEventListener('load', reflow);
    window.addEventListener('resize', reflow);
    if (window.ResizeObserver) {
      new ResizeObserver(reflow).observe(sidebar);
    }
    reflow();
  })();

  const SUPA = import.meta.env.PUBLIC_SUPABASE_URL;
  const KEY = import.meta.env.PUBLIC_SUPABASE_ANON_KEY;
  if (SUPA && KEY) {
    const headers = { apikey: KEY, Authorization: `Bearer ${KEY}` };
    Promise.all([
      fetch(`${SUPA}/rest/v1/baskets?select=pnl_usd,account_login&limit=100000`, { headers }).then((r) => r.json()),
      fetch(`${SUPA}/rest/v1/accounts?select=login,display_name`, { headers }).then((r) => r.json()).catch(() => []),
    ])
      .then(([rows, accs]) => {
        if (!Array.isArray(rows) || rows.length === 0) return;
        const names = {};
        if (Array.isArray(accs)) for (const a of accs) names[String(a.login)] = a.display_name || String(a.login);
        const money = (n) => (n >= 0 ? '+$' : '-$') + Math.abs(Math.round(n)).toLocaleString();
        const set = (id, v) => { const el = document.getElementById(id); if (el) el.textContent = v; };
        const render = (login) => {
          const sel = login == null ? rows : rows.filter((b) => String(b.account_login) === login);
          let sum = 0, win = 0;
          for (const b of sel) { const p = Number(b.pnl_usd) || 0; sum += p; if (p >= 0) win++; }
          const wr = sel.length ? Math.round((win / sel.length) * 100) : 0;
          const pnlEl = document.getElementById('tj-pnl');
          if (pnlEl) { pnlEl.textContent = money(sum); pnlEl.style.color = sum >= 0 ? '' : '#ff665d'; }
          set('tj-wr', wr + '%');
          set('tj-ct', String(sel.length));
        };
        // toggle รวม/ต่อพอร์ต — โชว์เมื่อมีมากกว่า 1 พอร์ตจริงในข้อมูล (label จาก accounts.display_name, fallback เลขบัญชี)
        const logins = [...new Set(rows.map((b) => (b.account_login == null ? '' : String(b.account_login))).filter(Boolean))].sort();
        const wrap = document.getElementById('tj-toggle');
        if (wrap && logins.length > 1) {
          const OFF = 'flex-1 rounded-md py-1 cursor-pointer bg-transparent text-gray-dark text-[11px] font-semibold border-0';
          const ON = 'flex-1 rounded-md py-1 cursor-pointer bg-primary text-white text-[11px] font-semibold border-0';
          const mk = (label, login) => {
            const btn = document.createElement('button');
            btn.type = 'button';
            btn.textContent = label;
            btn.className = OFF;
            btn.addEventListener('click', () => {
              wrap.querySelectorAll('button').forEach((b) => { b.className = OFF; });
              btn.className = ON;
              render(login);
            });
            wrap.appendChild(btn);
            return btn;
          };
          mk('รวม', null).className = ON;
          logins.forEach((l) => mk(names[l] || l, l));
          wrap.classList.remove('hidden');
          wrap.classList.add('flex');
        }
        render(null);
      })
      .catch(() => {});
  }
  // Myfxbook — API คาย array ทุกพอร์ต live; การ์ด SSR (มี 2 ใบตอน build) reuse ตามลำดับ พอร์ตเกินจากนั้น clone เพิ่มเอง
  fetch('https://www.intensivetrader.com/api/myfxbook.json')
    .then((r) => r.json())
    .then((raw) => {
      const list = document.getElementById('mfb-list');
      const ssrCards = list ? Array.from(list.querySelectorAll('[data-mfb-id]')) : [];
      const template = ssrCards[0];
      if (!list || !template) return;
      // API รุ่นเก่าคาย object พอร์ตเดียว (ไม่มี id/name) — ถือเป็นพอร์ต 1 (การ์ด SSR ใบแรก)
      let accounts = raw;
      if (!Array.isArray(accounts)) {
        if (!accounts || accounts.gain == null) return;
        const lbl = template.querySelector('.mfb-label');
        accounts = [{ id: template.getAttribute('data-mfb-id'), name: lbl ? (lbl.textContent || '').split(' · ')[0] : '', ...accounts }];
      }
      if (accounts.length === 0) return;
      const slugify = (s) => (s || 'account').toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/(^-|-$)/g, '');
      accounts.forEach((acc, i) => {
        // หาการ์ดตาม id ก่อน — ไม่เจอ (พอร์ตใหม่ที่ fallback ตอน build) ใช้การ์ด SSR ตามลำดับ — เกินจำนวน SSR ค่อย clone
        let card = list.querySelector(`[data-mfb-id="${acc.id}"]`) || ssrCards[i];
        let isNew = false;
        if (!card) {
          card = template.cloneNode(true);
          list.appendChild(card);
          isNew = true;
        }
        card.setAttribute('data-mfb-id', String(acc.id));
        const set = (sel, v, pre, suf) => { const el = card.querySelector(sel); if (el && v != null) el.textContent = (pre || '') + v + (suf || ''); };
        set('.mfb-gain', acc.gain, '+', '%');
        set('.mfb-absgain', acc.absGain, '+', '%');
        set('.mfb-monthly', acc.monthly, '', '%');
        set('.mfb-dd', acc.drawdown, '', '%');
        set('.mfb-balance', acc.balance, '$', '');
        const gainEl = card.querySelector('.mfb-gain');
        if (gainEl && acc.gain != null) gainEl.classList.add('positive');
        const absGainEl = card.querySelector('.mfb-absgain');
        if (absGainEl && acc.absGain != null) absGainEl.classList.add('positive');
        const label = card.querySelector('.mfb-label');
        if (label && acc.name) label.textContent = `${acc.name} · พอร์ต ${i + 1}`;
        // มีตัวเลขจริงแล้ว — เอา note "พอร์ตใหม่ รอผูก" ออก
        if (acc.gain != null) { const note = card.querySelector('.mfb-new-note'); if (note) note.remove(); }
        // การ์ด SSR ที่มีลิงก์จริงอยู่แล้วคงเดิม — การ์ด clone/การ์ด fallback (href="#") คำนวณลิงก์เอง
        const link = card.querySelector('.mfb-link');
        if (link && (isNew || link.getAttribute('href') === '#')) link.href = `https://www.myfxbook.com/members/kwasati/${slugify(acc.name)}/${acc.id}`;
      });
    })
    .catch(() => {});
  document.querySelectorAll('.copy-code').forEach((btn) => {
    btn.addEventListener('click', async () => {
      const code = btn.getAttribute('data-copy') || '';
      const fallbackCopy = () => {
        const input = document.createElement('textarea');
        input.value = code;
        input.setAttribute('readonly', '');
        input.style.position = 'fixed';
        input.style.opacity = '0';
        document.body.appendChild(input);
        input.select();
        const copied = document.execCommand('copy');
        input.remove();
        return copied;
      };
      let copied = false;
      if (navigator.clipboard && navigator.clipboard.writeText) {
        try {
          await navigator.clipboard.writeText(code);
          copied = true;
        } catch {
          copied = fallbackCopy();
        }
      } else {
        copied = fallbackCopy();
      }
      const prev = btn.textContent;
      btn.textContent = copied ? 'คัดลอกแล้ว!' : 'คัดลอกไม่สำเร็จ';
      setTimeout(() => { btn.textContent = prev; }, 1500);
    });
  });
</script>
</Layout>
